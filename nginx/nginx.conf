# Privacy Hub - Main NGINX Configuration
# This file orchestrates all nginx behavior and includes modular configs

user nginx;
worker_processes auto;
pid /tmp/nginx.pid;
error_log /dev/stderr warn;

events {
    # Optimized for Raspberry Pi - not too many connections
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

# DNS forwarding for Pi-hole - Commented out to prevent startup conflicts
# Uncomment and customize as needed when Pi-hole is stable
# stream {
#     upstream pihole_dns {
#         # Primary DNS - Pi-hole container
#         server pihole:53 max_fails=3 fail_timeout=10s;
#         
#         # Fallback DNS servers - multiple options for reliability
#         server 1.1.1.1:53 backup;      # Cloudflare DNS
#         server 1.0.0.1:53 backup;      # Cloudflare DNS secondary
#         server 8.8.8.8:53 backup;      # Google DNS
#         server 8.8.4.4:53 backup;      # Google DNS secondary
#         server 9.9.9.9:53 backup;      # Quad9 DNS
#         server 208.67.222.222:53 backup;  # OpenDNS
#     }
#     
#     server {
#         listen 53 udp;
#         proxy_pass pihole_dns;
#         proxy_timeout 3s;          # Increased timeout for reliability
#         proxy_responses 1;
#         proxy_bind $remote_addr transparent;  # Preserve client IP
#     }
#     
#     server {
#         listen 53;
#         proxy_pass pihole_dns;
#         proxy_timeout 3s;          # Increased timeout for reliability
#         proxy_bind $remote_addr transparent;  # Preserve client IP
#     }
# }

http {
    # Basic MIME type handling
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Performance optimizations for Raspberry Pi
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    
    # Hide nginx version for security
    server_tokens off;

    # Logging format with useful debugging info
    log_format detailed '$remote_addr - $remote_user [$time_local] '
                       '"$request" $status $body_bytes_sent '
                       '"$http_referer" "$http_user_agent" '
                       'rt=$request_time uct="$upstream_connect_time" '
                       'uht="$upstream_header_time" urt="$upstream_response_time"';
    
    access_log /dev/stdout detailed;

    # Basic optimizations
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types 
        text/plain
        text/css
        text/xml
        text/javascript
        application/javascript
        application/xml+rss
        application/json;

    # Include modular configurations in order
    include /etc/nginx/conf.d/*.conf;
    
    # Include active site configurations
    include /etc/nginx/sites-enabled/*.conf;
}